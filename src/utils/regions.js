export function isEurope() {
  return ["eune", "euw", "tr", "ru"];
}

export function isAmericas() {
  return "na" || "br" || "lan" || "las" || "oce";
}

export function isAsia() {
  return "kr" || "jp";
}
